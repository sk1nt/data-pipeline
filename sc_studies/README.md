# ACSIL studies (data-pipeline copy)

Sweep-related studies are **maintained in `data-trading/sc_studies/`** (`sweep_binary_bridge.cpp`, `sweep_dom_exporter.cpp`, `position_bridge.cpp`).

Files here are legacy copies. For new installs use the data-trading repo on the Sierra Chart machine.